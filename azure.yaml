# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: alexandria
services:  
  app:
    language: dotnet
    project: ./src/Alexandria.AppHost/Alexandria.AppHost.csproj
    host: containerapp
